name: Explicit TV vertical focus patch

on:
  push:
    branches:
      - fix/tv-explicit-vertical-focus

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/tv-explicit-vertical-focus
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/apply-tv-explicit-vertical-focus.py
      - name: Commit patch if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add app/src/tv/java/app/local1st/files/ui/browser/PaneView.kt
          git commit -m "Use explicit TV vertical list focus navigation"
          git push origin HEAD:fix/tv-explicit-vertical-focus
